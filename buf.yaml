# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
version: v2
modules:
  - path: proto
    # name: ts/dexhand
    lint:
      use:
        - STANDARD
    breaking:
      use:
        - FILE
lint:
  use:
    - STANDARD
breaking:
  use:
    - PACKAGE